# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

#font pango:monospace 8
font pango: Comfortaa 12

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
bindsym $mod+Shift+space floating toggle

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# i3 maintenance
bindsym $mod+Shift+r restart
bindsym $mod+Shift+c reload
bindsym $mod+Shift+e exit

# run menu
bindsym $mod+d exec --no-startup-id rofi -show run
# bindsym $mod+d exec --no-startup-id dmenu_run -i


# FreeBSD bindings for media keys
#bindsym XF86AudioRaiseVolume exec --no-startup-id mixer vol +5
#bindsym XF86AudioLowerVolume exec --no-startup-id mixer vol -5
#bindsym XF86AudioMute exec --no-startup-id mixer vol 0

# Fedora bindings for media keys
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer set Master 5%+
bindsym XF86AudioLowerVolume exec --no-startup-id amixer set Master 5%-
bindsym XF86AudioMute exec --no-startup-id amixer set Master toggle

# Brightness bindings 
# Requires light to be installed
#bindcode 233 exec --no-startup-id light -A 5 
#bindcode 232 exec --no-startup-id light -U 5
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 5
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 5

# use vi style navigation
set $left h
set $down j
set $up k
set $right l

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right


bindsym $mod+e split toggle
bindsym $mod+f fullscreen toggle


# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10



# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window<E2><80><99>s width.
        # Pressing right will grow the window<E2><80><99>s width.
        # Pressing up will shrink the window<E2><80><99>s height.
        # Pressing down will grow the window<E2><80><99>s height.
        bindsym $left       resize shrink width 10 px or 10 ppt
        bindsym $down       resize grow height 10 px or 10 ppt
        bindsym $up         resize shrink height 10 px or 10 ppt
        bindsym $right      resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# resize window (you can also use the mouse for that)
mode "window-mode" {

        # Pressing left will shrink the window<E2><80><99>s width.
        # Pressing right will grow the window<E2><80><99>s width.
        # Pressing up will shrink the window<E2><80><99>s height.
        # Pressing down will grow the window<E2><80><99>s height.
        bindsym $left       floating toggle
        bindsym $down       fullscreen toggle
#       bindsym $up         resize shrink height 10 px or 10 ppt
#       bindsym $right      resize grow width 10 px or 10 ppt

#       # same bindings, but for the arrow keys
#       bindsym Left        resize shrink width 10 px or 10 ppt
#       bindsym Down        resize grow height 10 px or 10 ppt
#       bindsym Up          resize shrink height 10 px or 10 ppt
#       bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+m mode "window-mode"

for_window [class="(?i)gimp"] floating enable

bindsym Ctrl+$alt+L  exec "scrot /tmp/i3lock.png; convert /tmp/i3lock.png -blur 0x8 /tmp/i3lock.png; i3lock -i /tmp/i3lock.png; rm /tmp/i3lock.png"



# special
set_from_resource $foreground i3wm.foreground   #c5c8c6
set_from_resource $background i3wm.background   #1d1d1d
set_from_resource $cursorColor i3wm.cursorColor  #c5c8c6

# black
set_from_resource $color0 i3wm.color0       #282a2e
set_from_resource $color8 i3wm.color8       #373b41

# red
set_from_resource $color1 i3wm.color1       #a54242
set_from_resource $color9 i3wm.color9       #cc6666

# green
set_from_resource $color2 i3wm.color2       #8c9440
set_from_resource $color10 i3wm.color10      #b5bd68

# yellow
set_from_resource $color3 i3wm.color3       #de935f
set_from_resource $color11 i3wm.color11      #f0c674

# blue
set_from_resource $color4 i3wm.color4       #5f819d
set_from_resource $color12 i3wm.color12      #81a2be

# magenta
set_from_resource $color5 i3wm.color5       #85678f
set_from_resource $color13 i3wm.color13      #b294bb

# cyan
set_from_resource $color6 i3wm.color6       #5e8d87
set_from_resource $color14 i3wm.color14      #8abeb7

# white
set_from_resource $color7 i3wm.color7       #707880
set_from_resource $color15 i3wm.color15      #c5c8c6

# class                 border		backgr		text    	indicator	child_border
client.focused          $background	$background	$foreground	$cursorColor 	$color7
client.focused_inactive $color0		$color0		$foreground	$color0		$color0
client.unfocused        $color0		$color0		$foreground	$color0		$color0
client.urgent           $color0		$color0		$foreground	$color0		$color0
client.placeholder      $color0		$color0		$foreground	$color0		$color0

client.background       $background

default_border none
hide_edge_borders both


bar {
    status_command i3status
    position top
    tray_output primary
    tray_padding 0
    font pango:Comfortaa 14
    strip_workspace_numbers yes
    binding_mode_indicator yes
    separator_symbol "|"
    colors{
        background $background
    }
}


exec_always --no-startup-id feh --bg-scale ~/.config/wallpapers/default

# network/wifi management from the try
exec --no-startup-id nm-applet &

# enable bluetooth management from the tray
#exec --no-startup-id blueman-applet &
exec --no-startup-id blueberry-tray &

# Enable late night blue lite filtering
exec --no-startup-id redshift-gtk &

# enable notification popups
exec --no-startup-id dunst &

# enable finger pad tap to click
exec --no-startup-id xinput set-prop 11 281 1

exec --no-startup-id volumeicon

# Enable composition
# Sure, it's cool, but its also unresonably power hungry
# exec --no-startup-id compton -CGb


exec_always --no-startup-id xrdb ~/.Xresources
exec_always --no-startup-id xrandr --output DP-3 --primary
#exec --no-startup-id xrandr --output DVI-D-0 --left-of DVI-I-1
